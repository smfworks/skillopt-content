# Examples

- `articles/` — tiny public sample posts for offline loop plumbing  
- After a successful run, compare `skill_template.md` with `best_skill.md` (generated at repo root)

These samples are **illustrative only**. They are not a benchmark suite.
